# Dataset Handling Skill

## Purpose
Keep dataset usage clean and predictable for the project team.

## Rules
- Expect the raw CSV under `data/raw/EV_Energy_Consumption_Dataset.csv`.
- Keep the raw file unchanged.
- Validate required columns in code instead of assuming the CSV is correct.
- Surface readable setup errors inside the app.
- Document provenance and local setup whenever dataset behavior changes.
